"use strict";

const express = require("express");

const ruleEngineController = require("../controllers/ruleEngine.controller");

const router = express.Router();

// router.get("/validate/:id", ruleEngineController);

module.exports = router;